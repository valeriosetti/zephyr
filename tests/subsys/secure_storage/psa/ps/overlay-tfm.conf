CONFIG_TFM_ITS_MAX_ASSET_SIZE_OVERRIDE=y
CONFIG_TFM_ITS_MAX_ASSET_SIZE=256

CONFIG_TFM_PARTITION_PROTECTED_STORAGE=y
CONFIG_TFM_PS_MAX_ASSET_SIZE_OVERRIDE=y
# That's the default value so we're not changing anything, but this way
# we can use that Kconfig in test.
CONFIG_TFM_PS_MAX_ASSET_SIZE=2048
